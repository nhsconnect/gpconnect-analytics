# get sorted list of csv files in folder and subfolders
find * -type f -name "*.csv" | sort 

# get ApiReader.AddFile statements
find * -type f -name "*.csv" | sort | tr / \\ | sed 's/.*/exec ApiReader.AddFile 2, "&";/'